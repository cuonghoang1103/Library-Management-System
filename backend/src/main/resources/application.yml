spring:
  application:
    name: library-management

  datasource:
    url: jdbc:postgresql://localhost:5433/library_db
    username: library_user
    password: library_pass
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

  flyway:
    enabled: false

  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:your-256-bit-secret-key-for-jwt-signing-must-be-long-enough}
    expiration-ms: 86400000
  seed:
    # Demo account passwords - no default: if unset, DataLoader generates a random one
    librarian-password: ${SEED_LIBRARIAN_PASSWORD:}
    member-password: ${SEED_MEMBER_PASSWORD:}

logging:
  level:
    root: INFO
    com.library: DEBUG
    org.springframework.security: INFO
